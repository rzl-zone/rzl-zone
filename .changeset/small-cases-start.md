---
"@rzl-zone/utils-js": minor
---

Refactored `getInitialsName` to introduce configuration options (`useLastWord`, `lowercaseSecondLetter`) with improved TypeScript safety, and added a new `getDisplayName` utility for flexible full name formatting featuring `useLastWord` and `capitalizeFirst` options, includes comprehensive test suites for both utilities.
