---
"@rzl-zone/click-feedback": major
---

CSS is no longer auto-injected, users must now explicitly import the CSS file from `@rzl-zone/click-feedback/styles` into their main application entry point (e.g., `app.tsx`, `app.jsx`) or global stylesheets.
