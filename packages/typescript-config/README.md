## Workspace TypeScript Config

Shared **TypeScript** configurations for the **Rzl Zone** monorepo workspace.

Provides reusable base settings and consistent compiler standards across internal packages and applications.
