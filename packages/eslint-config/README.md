## Workspace ESLint Config

Shared **ESLint** configurations for the **Rzl Zone** monorepo workspace.

Includes reusable presets for base projects, **Next.js**, and internal **React** applications to keep code quality consistent across packages.
